export class StorageConstants {
  public static readonly APP_SCHEME: string = "@basco:lognature-challenge/";

  public static readonly LOCALE: string = `${StorageConstants.APP_SCHEME}locale`;

  public static readonly TASKS: string = `${StorageConstants.APP_SCHEME}tasks`;
}
